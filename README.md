# xcoding-workflow

Portable, Skill-driven coding workflows. Each project has a `.xcoding/`
workshop; durable work is opened as a work order with its own workbench under
`.xcoding/work-orders/<work_order_id>/`.
