Run the complete repository regression contract for this node after all implementation repairs. Keep temporary files under the workbench `tmp/` directory, preserve the real Git index byte-for-byte, and use `python -B`. If a fixture requires an external non-Git root, prove that boundary first, record the exact path and reason, and verify cleanup. Record the exact command, test count, duration, failures, environmental gaps, and residual risks in the declared artifact. Complete only this node through the assigned terminal capability.
