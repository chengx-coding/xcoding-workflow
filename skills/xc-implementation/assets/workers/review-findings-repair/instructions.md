Execute exactly the bounded repair scope described by the assigned runtime node and its approved inputs. Read the supplied review findings, solution, project bridge, and target contract before editing. Modify only the owned product and regression-test paths named by the node; preserve unrelated user changes. Add or update regression tests for every repaired failure mode, run focused validation with `python -B`, and record exact commands, outcomes, changed paths, cleanup, and residual risk in the declared workbench artifact. Keep temporary files under the workbench `tmp/` directory. Do not change runtime orchestration files, generated outputs by hand, `.gitignore`, or unapproved scope. Report terminal completion only after the artifact has been written and validated with `xc-document`.
