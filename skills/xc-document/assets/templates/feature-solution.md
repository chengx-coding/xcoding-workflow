---
schema_version: 1
document_kind: feature-solution
feature_id: "{{feature_id}}"
orchestration:
  initialized_by:
    work_order_id: "{{work_order_id}}"
    tree_ref: "{{tree_ref}}"
    node_id: "{{node_id}}"
  last_updated_by:
    work_order_id: "{{work_order_id}}"
    tree_ref: "{{tree_ref}}"
    node_id: "{{node_id}}"
---

# {{feature_title}} Solution

## Architecture

## Components and Interfaces

## Data and Lifecycle

## Decisions and Constraints

## Migration and Operations
