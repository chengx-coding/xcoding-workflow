---
schema_version: 1
document_kind: feature-verification
feature_id: "{{feature_id}}"
orchestration:
  initialized_by:
    run_id: "{{run_id}}"
    tree_ref: "{{tree_ref}}"
    node_id: "{{node_id}}"
  last_updated_by:
    run_id: "{{run_id}}"
    tree_ref: "{{tree_ref}}"
    node_id: "{{node_id}}"
---

# {{feature_title}} Verification

## Requirement Mapping

## Test Scenarios

## Assertions and Environment

## Project Commands
